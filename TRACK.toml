version = 2
updated = "2026-05-08"
phase = "layered-pi-customization-profile"
goal = "Implement ADR 0012 layered Pi customization with opt-in package recommendations, explicit theme configuration, compact runtime hints, docs, and verification."
next_focus = "Phase 8 is verified; next active slice should be chosen from the remaining roadmap or release needs."

[tracking]
schema = "active-window-v1"
canonical_current_state = "TRACK.toml"
canonical_roadmap = "docs/roadmap/ROADMAP.md"
canonical_reference = "docs/reference/"
canonical_decision_history = "docs/adr/"
canonical_standards = "docs/standards/"
progress_evidence = "git commits and pull requests"
rules = [
  "Keep only active-window state here.",
  "Do not store completed work, hypotheses, research notes, or historical narrative here.",
  "Every work item must include why_now, write_scope, inputs, and done_when.",
  "Use docs/roadmap/ROADMAP.md for the full verified product checklist, docs/adr/ for durable decisions, docs/standards/ for durable rules, and docs/reference/ for reference context.",
  "Use commits and pull requests as progress evidence.",
  "Do not create parallel TODO, plan, memory, or research-dump files."
]

[current]
summary = "The active slice is ADR 0012's layered Pi customization profile after Phase 7 craft-router acceptance passed."
exit_criteria = [
  "Optional Pi package recommendations are configured with default_install = false while ADR 0010 defaults remain default-installed.",
  "The CLI lists and installs configured recommendations by ID through pi install with fixture-safe tests.",
  "The CLI can explicitly set theme = github-dark-pro in a supplied/temp Pi settings file without overwriting unrelated settings.",
  "Runtime hints remain compact and testable.",
  "README documents default packages, optional recommendations, and explicit configure/package commands.",
  "Phase 8 verification commands pass."
]
refs = [
  "docs/adr/0012-use-layered-pi-customization-profile.md",
  "docs/adr/0010-use-curated-default-pi-packages.md",
  "docs/roadmap/ROADMAP.md#phase-8--layered-pi-customization-profile",
  "docs/standards/INSTRUCTION-SURFACE-STANDARD.md",
]

[[work_items]]
id = "layered-pi-customization-profile"
status = "done"
title = "Add ADR 0012 layered Pi customization behavior"
why_now = "Phase 7 craft-router behavior is implemented and acceptance has passed, unblocking ADR 0012 Phase 8 work."
write_scope = [
  "pi-plugin/config-schema.toml",
  "pi-plugin/plugin.js",
  "pi-plugin/index.ts",
  "pi-plugin/plugin.test.js",
  "cli/",
  "README.md",
  "docs/roadmap/ROADMAP.md",
  "TRACK.toml",
]
inputs = [
  "docs/adr/0012-use-layered-pi-customization-profile.md",
  "docs/adr/0010-use-curated-default-pi-packages.md",
  "docs/standards/INSTRUCTION-SURFACE-STANDARD.md",
]
done_when = [
  "Optional recommendations are in config with default_install = false.",
  "package list/install and configure --theme have fixture-safe tests.",
  "cd cli && go test ./... passes.",
  "node --test pi-plugin/plugin.test.js passes.",
  "cd cli && ./acceptance.sh passes."
]
