# Roadmap

This directory owns product direction and verification evidence.

Use `ROADMAP.md` as the full product ledger: completed phases, remaining runtime/operator checks, and evidence required before any unchecked item is marked done.

`TRACK.toml` owns the active execution window. When a roadmap item becomes active, mirror only that bounded slice in `TRACK.toml`.
