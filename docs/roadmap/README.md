# Roadmap

This directory owns product direction and release discipline.

Use `ROADMAP.md` for current state, prioritized direction, and evidence for open or completed roadmap items. It is not the active task ledger.

`TRACK.toml` owns the active execution window. When a roadmap item becomes active, mirror only that bounded slice in `TRACK.toml`.
