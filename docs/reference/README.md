# Reference

This directory contains stable background that can inform decisions but is not the active plan or policy surface.

Use reference docs for source-grounded context, postmortems, or external research summaries that remain useful across sessions.

Current file:

- `OLD-SANE-POSTMORTEM.md` — read-only lessons from old Sane; do not treat it as current product behavior.

If reference material stops affecting decisions or verification, delete it instead of archiving more stale context.
